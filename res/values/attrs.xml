<?xml version="1.0" encoding="utf-8"?>
<resources>

    <declare-styleable name="CropImage">

        <!-- Identifier for the image that represents the Imageview's content. -->
        <attr name="image" format="reference" />
        <!-- crop Imageview's content same as mask -->
        <attr name="crop" format="reference" />
        <!-- Background Image -->
        <attr name="frame" format="reference" />
        <attr name="temp" format="reference"/>
    </declare-styleable>

</resources>